name: Build

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - 'sixteen'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04

    strategy:
      fail-fast: false
      matrix:
        target: [dm1q]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
        with:
          submodules: 'recursive'

      - name: Free disk space (1/3)
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo docker builder prune -a

      - name: Free disk space (2/3)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true

      - name: Free disk space (3/3)
        uses: rokibhasansagar/slimhub_actions@main
        with:
          retain: 'compiler_cmake'

      - name: Set up build environment
        run: |
          sudo apt update
          DEBIAN_FRONTEND=noninteractive sudo apt install -yq \
            7zip attr ccache clang ffmpeg jq \
            libbrotli-dev libgtest-dev libprotobuf-dev libunwind-dev libpcre2-dev \
            libzstd-dev linux-modules-extra-$(uname -r) lld protobuf-compiler webp
          sudo modprobe erofs f2fs
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          echo -n "${{ secrets.PLATFORM_KEY_PK8 }}" | base64 --decode > security/unica_platform.pk8
          echo -n "${{ secrets.PLATFORM_KEY_PEM }}" | base64 --decode > security/unica_platform.x509.pem

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Build dependencies
        run: |
          source ./buildenv.sh ${{ matrix.target }}
          ./scripts/build_dependencies.sh

      # =========================================================================
      # 🚀 [허깅페이스 직공 스트림] 트래픽 차단 및 유료 제한 없는 다운로드 파이프라인
      # =========================================================================
      - name: Hugging Face Pure Direct Download
        run: |
          rm -rf src/firmware/SM-S911N_KOO
          mkdir -p src/firmware/SM-S911N_KOO

          HF_DIRECT_85="https://huggingface.co/datasets/ssch11/S911N85un1ca1/resolve/main/SAMFW.COM_SM-S911N_KOO_S911NKSS8FZF1_fac.zip"
          HF_DIRECT_80="https://huggingface.co/datasets/ssch11/S911N85un1ca1/resolve/main/SAMFW.COM_SM-S911N_KOO_S911NKSS6EZA1_fac.zip"

          echo "📥 1. 포팅 베이스 One UI 8.5 다운로드 중 (Hugging Face)..."
          curl -L "${HF_DIRECT_85}" -o "src/firmware/SM-S911N_KOO/SM-S911N_5_20260612073910_b08t3v3kt6_fac.zip"

          echo "📥 2. 디바이스 타겟 One UI 8.0 v8 다운로드 중 (Hugging Face)..."
          curl -L "${HF_DIRECT_80}" -o "src/firmware/SM-S911N_KOO/SM-S911N_5_20260101120000_target_fac.zip"

          echo "✅ 다운로드 완료! 실제 파일 매핑 크기 체크:"
          ls -lh src/firmware/SM-S911N_KOO/

      # =========================================================================
      # 🛠️ 수동 압축 해제 및 오딘 캐시 인젝션 (스킵 오버라이드 락 해제)
      # =========================================================================
      - name: Force Unpack To Match Builder Environment
        run: |
          echo "🤖 빌더 내장 복사 세션 수동 개방..."
          rm -rf out/odin/SM-S911N_KOO
          mkdir -p out/odin/SM-S911N_KOO
          
          unzip -o src/firmware/SM-S911N_KOO/SM-S911N_5_20260612073910_b08t3v3kt6_fac.zip -d out/odin/SM-S911N_KOO/
          unzip -o src/firmware/SM-S911N_KOO/SM-S911N_5_20260101120000_target_fac.zip -d out/odin/SM-S911N_KOO/
          
          rm -f out/odin/SM-S911N_KOO/.downloaded
          rm -f out/odin/SM-S911N_KOO/.extracted
          
          echo "🤖 해제 완료된 리얼 바이너리 타르볼 리스트:"
          ls -lh out/odin/SM-S911N_KOO/

      # =========================================================================
      # 🛠️ 빌더 스크립트 우회 - 이미 추출되었다는 예외 로직 강제 삭제
      # =========================================================================
      - name: Patch Builder Script To Force Extraction
        run: |
          echo "🤖 빌더 스크립트에서 추출 스킵 조건문 비활성화 중..."
          if [ -f "scripts/extract_fw.sh" ]; then
            sed -i 's/exit 0/# exit 0/g' scripts/extract_fw.sh
            sed -i 's/return 0/# return 0/g' scripts/extract_fw.sh
          fi

      - name: Extract stock firmwares (Pure Build Stream)
        run: |
          source ./buildenv.sh ${{ matrix.target }}
          ./scripts/extract_fw.sh

      # =========================================================================
      # 🛠️ 순정 ROM 컴파일 파이프라인
      # =========================================================================
      - name: Build ROM
        run: |
          source ./buildenv.sh ${{ matrix.target }}
          ./scripts/make_rom.sh

      - name: Upload to Gofile
        run: |
          ROM_FILE=$(find out/ -type f \( -name "UN1CA_*.zip" -o -name "*-sign.zip" -o -name "*-ota-*.zip" \) -size +100M | grep -v "target_files" | head -n 1)
          
          if [ -z "$ROM_FILE" ]; then
            ROM_FILE=$(find out/ -type f \( -name "*.zip" -o -name "*.tar" \) -size +100M | grep -v "target_files" | head -n 1)
          fi
          
          if [ -z "$ROM_FILE" ]; then
            ROM_FILE=$(find . -type f \( -name "*.zip" -o -name "*.tar" \) -size +100M | head -n 1)
          fi

          if [ -z "$ROM_FILE" ]; then
            echo "❌ 100MB 이상의 빌드된 롬 파일을 찾을 수 없습니다."
            exit 1
          fi
          
          FILE_SIZE=$(du -sh "$ROM_FILE" | cut -f1)
          echo "진짜 롬 파일 찾음: $ROM_FILE ($FILE_SIZE)"
          SERVER=$(curl -s https://api.gofile.io/servers | jq -r '.data.servers[0].name')
          echo "Gofile 업로드 시작..."
          DOWNLOAD_LINK=$(curl -# -F "file=@$ROM_FILE" "https://${SERVER}.gofile.io/uploadFile" | jq -r '.data.downloadPage')
          echo "===================================================="
          echo " 🎉 빌드가 성공적으로 Gofile에 업로드 되었습니다!"
          echo " 다운로드 링크: $DOWNLOAD_LINK"
          echo "===================================================="
